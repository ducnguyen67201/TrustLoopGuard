---
title: Team
full: true
_openapi:
  preload:
    - ../../docs/openapi.yaml
  toc:
    - depth: 2
      title: GET /v1/team/my-workspaces — list workspaces for the signed-in user.
      url: '#get-v1teammy-workspaces--list-workspaces-for-the-signed-in-user'
    - depth: 2
      title: |-
        POST /v1/team/my-workspaces — create a new workspace owned by
        the caller. Bootstraps a fresh organization too, so a user who
        signed up without an invite can self-serve.
      url: >-
        #post-v1teammy-workspaces--create-a-new-workspace-owned-bythe-caller-bootstraps-a-fresh-organization-too-so-a-user-whosigned-up-without-an-invite-can-self-serve
  structuredData:
    headings:
      - content: GET /v1/team/my-workspaces — list workspaces for the signed-in user.
        id: get-v1teammy-workspaces--list-workspaces-for-the-signed-in-user
      - content: |-
          POST /v1/team/my-workspaces — create a new workspace owned by
          the caller. Bootstraps a fresh organization too, so a user who
          signed up without an invite can self-serve.
        id: >-
          post-v1teammy-workspaces--create-a-new-workspace-owned-bythe-caller-bootstraps-a-fresh-organization-too-so-a-user-whosigned-up-without-an-invite-can-self-serve
    contents:
      - content: |-
          Reads `X-TLG-User-Id` (required, UUID) and `X-TLG-User-Email`
          (optional). When the email is present we first bulk-accept any
          pending invites addressed to it; the membership query then sees
          the new rows in the same response. This is the dashboard's
          "auto-bind on next request" mechanism.
        heading: get-v1teammy-workspaces--list-workspaces-for-the-signed-in-user
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="../../docs/openapi.yaml" webhooks={[]} operations={[{"path":"/v1/team/my-workspaces","method":"get"},{"path":"/v1/team/my-workspaces","method":"post"}]} showTitle showDescription />
    </>
  );
}