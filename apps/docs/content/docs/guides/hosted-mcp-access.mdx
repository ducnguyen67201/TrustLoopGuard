---
title: Hosted MCP Access
description: Connect employee AI clients to assigned remote MCP tools through OAuth and live TrustLoopGuard policy enforcement.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

Hosted MCP Access gives a workspace one managed MCP endpoint. Employees connect an OAuth-capable
AI client to that endpoint instead of connecting directly to every remote MCP server.
TrustLoopGuard uses the signed-in member to personalize tool discovery, then evaluates every tool
call against current assignments and runtime policy before contacting the upstream server.

![Hosted MCP access gateway](/diagrams/hosted-mcp-access-gateway.svg)

> MCP access governs only calls routed through this hosted MCP endpoint. It does not install
> universal interception for native or third-party tools in Claude Code, Codex, or OpenCode. Use
> the [coding-agent tool gate](/docs/guides/coding-agent-tool-gates) for that boundary.

## Before you begin

You need:

- A TrustLoopGuard workspace with **MCP Access** enabled.
- Owner or Admin access to configure servers and assignments.
- A publicly reachable HTTPS Streamable HTTP MCP server. TrustLoopGuard does not launch stdio
  commands or connect to legacy HTTP+SSE servers.
- An OAuth-capable Streamable HTTP client. Use one of the verified clients below, or another client
  that supports OAuth discovery, dynamic client registration, and PKCE.

TrustLoop Cloud uses this managed endpoint:

```text
https://api.gettrustloop.app/mcp
```

Self-hosted installations use `$TL_PUBLIC_URL/mcp` and must configure `TL_PUBLIC_URL`,
`TL_DASHBOARD_URL`, `TL_JWT_SECRET`, and `TL_GATEWAY_CREDENTIAL_KEY` on the Rust server.

## Connect a remote MCP server

1. Open your workspace in the TrustLoopGuard dashboard and select the intended environment.
2. Open **MCP Access → Servers**.
3. Select **Connect server**.
4. Enter a display name, stable slug, and the server's full HTTPS MCP endpoint.
5. Add its bearer token when the upstream requires one. The credential is encrypted and is never
   displayed again.
6. Select **Connect and sync**.

A successful sync pins the upstream tool names, descriptions, annotations, schemas, and schema
hashes. Review the server row and confirm that **Last sync** says **Succeeded** before assigning
tools.

## Assign tools to members

Open **MCP Access → Tool access**, then:

1. Select the member who will complete OAuth.
2. Review each discovered tool and classify its side effect, such as **Read** or **API mutation**.
3. Grant only the tools that member should discover and call.

Assignments control discovery, but they are not the only authorization boundary. Runtime policy
still evaluates every assigned `tools/call`.

## Connect an AI client

Choose your client. Each option connects to the same managed endpoint and opens the
TrustLoopGuard authorization page in your browser.

<Tabs
  items={[
    'Codex CLI',
    'Claude Code',
    'Cursor',
    'OpenCode',
    'Hermes',
    'Gemini CLI',
    'VS Code',
    'Windsurf',
    'Claude',
    'Other clients',
  ]}
>
  <Tab value="Codex CLI">
    Add the managed endpoint once:

    ```sh
    codex mcp add trustloopguard \
      --url https://api.gettrustloop.app/mcp \
      --oauth-resource https://api.gettrustloop.app/mcp
    ```

    Complete OAuth:

    ```sh
    codex mcp login trustloopguard --scopes mcp:tools
    ```

    Confirm that the server is connected:

    ```sh
    codex mcp list
    ```

  </Tab>

  <Tab value="Claude Code">
    Add the managed endpoint to your user configuration:

    ```sh
    claude mcp add --scope user --transport http \
      trustloopguard https://api.gettrustloop.app/mcp
    ```

    Start Claude Code, run `/mcp`, select `trustloopguard`, and choose **Authenticate** to complete
    OAuth in your browser.

    Confirm that the server is connected:

    ```sh
    claude mcp list
    ```

  </Tab>

  <Tab value="Cursor">
    Open the global MCP configuration from **Cursor Settings → Tools & Integrations → MCP Tools**,
    or create `.cursor/mcp.json` for one project:

    ```json
    {
      "mcpServers": {
        "trustloopguard": {
          "url": "https://api.gettrustloop.app/mcp"
        }
      }
    }
    ```

    Enable `trustloopguard` in MCP Tools and complete the browser sign-in. Cursor Agent users can
    trigger OAuth and inspect the connected catalog from the terminal:

    ```sh
    cursor-agent mcp login trustloopguard
    cursor-agent mcp list-tools trustloopguard
    ```

  </Tab>

  <Tab value="OpenCode">
    Add the remote server to `opencode.json`:

    ```json
    {
      "$schema": "https://opencode.ai/config.json",
      "mcp": {
        "trustloopguard": {
          "type": "remote",
          "url": "https://api.gettrustloop.app/mcp",
          "enabled": true
        }
      }
    }
    ```

    Complete OAuth and confirm the connection:

    ```sh
    opencode mcp auth trustloopguard
    opencode mcp list
    ```

  </Tab>

  <Tab value="Hermes">
    Add TrustLoopGuard to `~/.hermes/config.yaml`:

    ```yaml
    mcp_servers:
      trustloopguard:
        url: "https://api.gettrustloop.app/mcp"
        auth: oauth
    ```

    Complete OAuth from a fresh terminal, then start a new Hermes session:

    ```sh
    hermes mcp login trustloopguard
    hermes chat
    ```

    Hermes supports browser callbacks, paste-back authorization for remote hosts, and PKCE with
    dynamic client registration.

  </Tab>

  <Tab value="Gemini CLI">
    Add the managed endpoint to your user configuration:

    ```sh
    gemini mcp add --scope user --transport http \
      trustloopguard https://api.gettrustloop.app/mcp
    ```

    Start Gemini CLI and complete OAuth:

    ```text
    /mcp auth trustloopguard
    /mcp list
    ```

  </Tab>

  <Tab value="VS Code">
    Run **MCP: Open User Configuration** from the Command Palette, or create `.vscode/mcp.json`
    for one workspace:

    ```json
    {
      "servers": {
        "trustloopguard": {
          "type": "http",
          "url": "https://api.gettrustloop.app/mcp"
        }
      }
    }
    ```

    Run **MCP: List Servers**, start `trustloopguard`, and complete the browser authorization flow.
    Use **MCP: Reset Cached Tools** after a grant when you need to refresh discovery immediately.

  </Tab>

  <Tab value="Windsurf">
    Open **Windsurf Settings → Cascade → MCP Servers → View Raw Config**, then add:

    ```json
    {
      "mcpServers": {
        "trustloopguard": {
          "serverUrl": "https://api.gettrustloop.app/mcp"
        }
      }
    }
    ```

    Refresh the MCP list, select `trustloopguard`, and complete OAuth in your browser.

  </Tab>

  <Tab value="Claude">
    Claude web and Claude Desktop connect to remote MCP servers through custom connectors:

    1. Open **Customize → Connectors**.
    2. Select **Add custom connector**. Team and Enterprise workspaces require an Owner to add it
       from **Organization settings → Connectors** first.
    3. Name it `TrustLoopGuard` and enter:

       ```text
       https://api.gettrustloop.app/mcp
       ```

    4. Select **Connect**, complete OAuth, and enable the connector for the conversation.

  </Tab>

  <Tab value="Other clients">
    Use this endpoint in any client that supports remote MCP over Streamable HTTP and OAuth 2.1:

    ```text
    https://api.gettrustloop.app/mcp
    ```

    The client must support protected-resource and authorization-server discovery, dynamic client
    registration, PKCE with `S256`, browser authorization, refresh tokens, and the `mcp:tools`
    scope. A client that only accepts a static bearer token is not compatible with employee OAuth.

  </Tab>
</Tabs>

Sign in, select the prepared workspace, and approve access when the authorization page opens.
Then start a fresh client session so it discovers the current assigned catalog. An explicit first
prompt is useful when demonstrating a particular tool:

```text
Use the `trustloopguard` MCP server. Call `company__read_engineering_docs`
with {"topic":"intern onboarding"}. Do not inspect local files or run shell commands.
```

The public tool name includes the server's stable slug so tools from multiple upstream servers do
not collide.

## How access changes affect active clients

MCP discovery and MCP execution are separate:

```text
Client starts
  → tools/list
  → TrustLoopGuard returns the member's active assigned tools
  → the client registers those tool schemas for the model session

Model selects a known tool
  → tools/call
  → TrustLoopGuard rechecks membership and assignment
  → TrustLoopGuard evaluates current runtime policy
  → the upstream server is called only after permit
```

The current hosted gateway does not send `notifications/tools/list_changed` when an administrator
changes assignments. Clients such as Codex can therefore retain their session's earlier discovery
result.

| Administrative change          | Existing client session                             | Server behavior on the next call                 |
| ------------------------------ | --------------------------------------------------- | ------------------------------------------------ |
| Grant a previously hidden tool | The tool remains hidden until the client refreshes. | A direct call is allowed only after live checks. |
| Revoke a visible tool          | The stale tool may remain visible in the client.    | TrustLoopGuard denies the call immediately.      |
| Change runtime policy          | The visible catalog does not change.                | The current policy is evaluated immediately.     |
| Remove workspace membership    | The stale catalog may remain visible in the client. | Every MCP request is rejected.                   |

After granting a new tool, exit and restart the AI client to refresh discovery. The OAuth login
remains stored; you do not need to authorize again. Clearing the terminal display or starting a new
prompt in the same session does not refresh the catalog.

A stale client catalog is not an authorization grant. TrustLoopGuard resolves the current tool
assignment when `tools/call` arrives and rechecks authority immediately before upstream execution.
A revoked tool cannot execute even when an older client still displays it.

## Runtime enforcement

For every assigned call, TrustLoopGuard:

1. Rechecks the OAuth member's workspace access and the workspace feature flag.
2. Resolves the current assignment and pinned tool schema.
3. Validates the proposed arguments.
4. Verifies the live upstream schema still matches the pinned catalog.
5. Submits a server-authored tool event to the current environment's policy runtime.
6. Calls the upstream only after a `permit`, or after a required approval is granted and rechecked.
7. Re-reads assignment and connection authority immediately before execution.

`deny`, `defer`, and unsupported transformations return an MCP tool error without calling the
upstream server. Approved calls are also re-evaluated before execution.

## Troubleshooting

- **MCP Access is missing** — ask a workspace operator to enable the hosted MCP feature for the
  workspace.
- **Server synchronization fails** — verify that the complete endpoint is public HTTPS, implements
  Streamable HTTP, and accepts `initialize` and `tools/list`.
- **OAuth succeeds but a tool is missing** — confirm the tool is active, assigned to the exact OAuth
  member, and configured in the environment shown on the **Connect** tab; then restart the client.
- **A revoked tool is still displayed** — this can be stale client discovery. Calling it will still
  fail the live assignment check.
- **A policy does not block a call** — verify the tool's side-effect classification and that the
  policy is enabled in the environment named on the **Connect** tab.
- **Another MCP server fails during Codex startup** — failures from unrelated integrations do not
  invalidate a successful TrustLoopGuard OAuth login. Disable or repair those integrations
  separately.
