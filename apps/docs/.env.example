# Public origin used by /llms.txt, /llms-full.txt, and /robots.txt for
# the absolute URLs they emit. In production this should be the
# user-facing domain (e.g. https://trustloopguard.dev), even when the
# docs deployment itself lives on a different origin behind a rewrite.
# Defaults to http://localhost:3001 for local dev.
# NEXT_PUBLIC_SITE_URL=https://trustloopguard.dev
