# tl-server base URL the playground hits. Override per environment.
# Public because the browser performs the request directly; do not put
# privileged tokens here.
NEXT_PUBLIC_TL_SERVER_URL=http://localhost:8080
