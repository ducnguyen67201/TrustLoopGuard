{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SourceLabelPolicy",
  "description": "One workspace override row: per-origin label defaults. A family left `None` inherits the built-in origin default.",
  "type": "object",
  "required": [
    "origin"
  ],
  "properties": {
    "confidentiality": {
      "anyOf": [
        {
          "$ref": "#/definitions/Confidentiality"
        },
        {
          "type": "null"
        }
      ]
    },
    "integrity": {
      "anyOf": [
        {
          "$ref": "#/definitions/Integrity"
        },
        {
          "type": "null"
        }
      ]
    },
    "origin": {
      "$ref": "#/definitions/Origin"
    },
    "trust": {
      "anyOf": [
        {
          "$ref": "#/definitions/Trust"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "definitions": {
    "Confidentiality": {
      "type": "string",
      "enum": [
        "public",
        "private",
        "secret",
        "identity",
        "unknown"
      ]
    },
    "Integrity": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high",
        "unknown"
      ]
    },
    "Origin": {
      "type": "string",
      "enum": [
        "user",
        "system",
        "tool",
        "memory",
        "file",
        "web",
        "email",
        "api",
        "unknown"
      ]
    },
    "Trust": {
      "type": "string",
      "enum": [
        "trusted",
        "untrusted",
        "unknown"
      ]
    }
  }
}
