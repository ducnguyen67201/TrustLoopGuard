[package]
name = "tl-replay"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "TrustLoopGuard replay: re-run stored decisions against a new policy version"

[dependencies]
tl-core = { workspace = true }
tl-engine = { workspace = true }
serde = { workspace = true }
