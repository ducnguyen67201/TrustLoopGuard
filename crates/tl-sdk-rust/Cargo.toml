[package]
name = "tl-sdk-rust"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "TrustLoopGuard Rust SDK"

[dependencies]
tl-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
# `rt` is needed for the fire-and-forget `record_event` spawn.
tokio = { workspace = true, features = ["time", "rt"] }
tracing = { workspace = true }
rand = "0.10"
urlencoding = "2"
uuid = { workspace = true }

[dev-dependencies]
# Local mock HTTP server for integration tests of retry / auth wiring.
# Test-only — not pulled into the published SDK.
wiremock = "0.6"
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "time"] }
tracing-subscriber = { workspace = true }
